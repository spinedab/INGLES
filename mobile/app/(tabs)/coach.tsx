// ═══ Coach tab ═══
// Redirige al coach.tsx principal (que ya existe como ruta stack).
// Esto permite que el tab "Coach" muestre la pantalla completa.
import CoachScreen from '../coach';
export default CoachScreen;
